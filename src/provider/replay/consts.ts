/** MIME type used to tag replay marker data parts in assistant history. */
export const REPLAY_MARKER_MIME = 'stateful_marker';

/** Writer id prefix embedded in replay marker payloads. */
export const REPLAY_MARKER_WRITER_ID = 'copilot-model-hub';
